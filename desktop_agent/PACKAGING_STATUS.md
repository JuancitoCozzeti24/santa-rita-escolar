# Packaging status

The Windows packaging workflow is configured in `.github/workflows/build-sieroom-desktop-agent.yml`.
It builds `SIEROOM-Desktop-Agent-v0.1.exe` on `windows-latest` with PyInstaller and uploads it as the `SIEROOM-Desktop-Agent-Windows-v0.1` artifact.
